import { Bytes } from '../types/index.js';
import { Address } from './address.js';
export declare class RoochAddress implements Address {
    private readonly address;
    constructor(address: Bytes | string);
    toStr(): string;
    toBytes(): Bytes;
    toHexAddress(): string;
    toBech32Address(): string;
}
